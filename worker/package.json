{
  "name": "quest-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute quest_db --file=./schema.sql",
    "db:init:local": "wrangler d1 execute quest_db --local --file=./schema.sql"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}